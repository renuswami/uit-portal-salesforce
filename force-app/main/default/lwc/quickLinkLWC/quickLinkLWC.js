import { LightningElement, track } from 'lwc';

export default class QuickLinkBox extends LightningElement {
    
    expenseReimbursementUrl = '/s/expense-reimbursement'; 

    // Note: If have many links, would define an array here and loop through it in the HTML.
}